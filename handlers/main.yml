---
# handlers/main.yml

- name: Restart journald
  become: true
  ansible.builtin.systemd:
    name: systemd-journald
    state: restarted

- name: Restart docker
  become: true
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true
